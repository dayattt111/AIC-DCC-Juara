# =============================================================
# Kopita Minimum Requirements (Khusus CPU Lokal Super Ringan)
# =============================================================
# Menggunakan wheel CPU murni (menghemat ~2.5 GB paket CUDA NVIDIA).
# Hanya memuat dependensi esensial untuk menjalankan Backend FastAPI
# dan Frontend Streamlit tanpa library training yang berat.

--extra-index-url https://download.pytorch.org/whl/cpu

# 1. PyTorch & Torchvision Khusus CPU Murni (Ringan & Cepat)
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# 2. Image Processing
Pillow>=10.2.0

# 3. Backend FastAPI Engine
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9

# 4. Frontend Streamlit Portal
streamlit>=1.35.0
requests>=2.31.0
